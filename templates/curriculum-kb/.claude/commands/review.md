---
description: 정비 — 인박스 비우기, 약한 노드 보강, 오개념 확충, 허브 갱신
---

`CLAUDE.md` §6 Review 워크플로우를 따른다.

1. `00-inbox/` 각 메모를 노드로 승격(`/ingest` 절차)하거나 폐기 제안.
2. `status: seedling` 노드 점검 — 원문 대조·검수된 것은 `growing`/`evergreen` 승격 제안.
3. **오개념 확충**: 수업에서 관찰한 학생 오류를 사용자에게 물어 misconception 노드로
   기록(`contradicts::` + `requires::` 필수).
4. topic 허브 갱신, `index.md` 동기화.
5. `log.md`에 `review` 줄 append.

먼저 할 일 목록을 제시하고 사용자와 우선순위를 정한 뒤 진행한다. `10-sources/` 불변.
